# === core numerics / data ===
numpy>=1.23
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# === PyTorch (CPU/MPS by default; install CUDA wheel manually if needed) ===
torch>=2.2,<3

# === TensorFlow (for the fNN .h5 model) ===
# non-mac/Intel/AMD:
tensorflow>=2.13,<2.17 ; platform_system != "Darwin" or platform_machine != "arm64"
# apple silicon (M1/M2/M3):
tensorflow-macos>=2.13,<2.17 ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal>=1.1 ; platform_system == "Darwin" and platform_machine == "arm64"

# === HDF5 I/O for .h5 model ===
h5py>=3.8

# === optional niceties ===
tqdm>=4.66